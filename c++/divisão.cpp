#include <bits/stdc++.h>

using namespace std;

int main()
{
    double a, b;

    cin >> a >> b;

    double c = a/b;

    cout.precision(2);
    cout.setf(ios::fixed);
    cout << c;

    return 0;
}
