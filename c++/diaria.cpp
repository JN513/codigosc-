#include <bits/stdc++.h>

using namespace std;

int n, q;

pair<int,int> intervalo[ 100005];

int main(void){
    cin >> n;

    for(int i = 0, a, b; i < n; i++){
        cin >> a >> b;
        intervalo[i] = {a, b};
    }

    cin >> q;

    for (int i = 0; i < q; i++){
        
    }

    return 0;
}